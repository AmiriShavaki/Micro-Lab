
;Slide No 
.include "m32def.inc"
		LDI		R16,HIGH(RAMEND)
		OUT		SPH,R16
		LDI		R16,LOW(RAMEND)
		OUT		SPL,R16

		LDI		R16,0b00001000
		OUT		DDRB,R16

		LDI		R16,0b01101010
		OUT		TCCR0,R16

		LDI		R16,191
		OUT		OCR0,R16

HERE:	RJMP	HERE

